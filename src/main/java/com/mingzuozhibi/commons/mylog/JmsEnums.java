package com.mingzuozhibi.commons.mylog;

public class JmsEnums {
    public enum Type {
        DEBUG,
        INFO,
        NOTIFY,
        SUCCESS,
        WARNING,
        ERROR,
    }

    public enum Name {
        SPIDER_CONTENT,
        SPIDER_HISTORY,
        SERVER_DISC,
        SERVER_USER,
        SERVER_CORE,
        DEFAULT,
    }
}
